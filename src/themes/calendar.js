import { INCLUDE } from '@shoutem/theme';

export default (variables) => ({
  'lh.ui.Calendar': {
    iconColor: variables.darkColor,
    markedDayColor: variables.featuredColor,
    height: 370,
  },
});
