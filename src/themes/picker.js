import { INCLUDE } from '@shoutem/theme';

export default (variables) => ({
  'lh.ui.Picker': {
    item: {
      fontSize: 22,
      fontWeight: 'bold',
      color: variables.darkColor,
    },
  },
});
